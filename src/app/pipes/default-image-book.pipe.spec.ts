import { DefaultImageBookPipe } from './default-image-book.pipe';

describe('DefaultImageBookPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultImageBookPipe();
    expect(pipe).toBeTruthy();
  });
});
